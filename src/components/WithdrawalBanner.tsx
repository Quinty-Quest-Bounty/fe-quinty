// V2 contracts use push-based payments, so there are no pending withdrawals.
// This component renders nothing but is kept as a stub to avoid breaking imports.

export function WithdrawalBanner() {
    return null;
}
